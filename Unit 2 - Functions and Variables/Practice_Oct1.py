# 2 basic functions
# you know something is afunction because () at the end
# the suff that goesinto those () are called parameters
# Parameters are information the function needs to run
# I say jump you say ow high is the parameter
print("Hello World")    #"Hello World!" is the parameter
input("what to do \n>")
# \n is called an escape character (line break)
# input is never required

#variables
#variables a way to store data
# syntax (grammar)
# <name> = <value>
x = 5   # x is the variable name, 5 is the value
# Snake naming convetion - all lowercase, underscore for spaces
#comcis : short descriptive
username = "caleb"
